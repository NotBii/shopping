<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zerobase.shopping.mapper.AccountMapper">
  <!-- 회원가입 -->
  <insert id="signup">
    INSERT INTO account(userid, password, nickname, mail, role)
    VALUES (#{userid}, #{password}, #{nickname}, #{mail}, #{role})
  </insert>
  <!-- id 중복확인 -->
  <select id="idCheck" resultType = "boolean">
    SELECT IF(COUNT(userid) = 1, 1, 0) FROM account
    WHERE userid = #{userid}
  </select>
  <!-- mail 중복확인 -->
  <select id ="mailCheck" resultType = "boolean">
    SELECT IF(COUNT(mail) = 1, 1, 0) FROM account
    WHERE mail = #{mail}
  </select>

  <select id="userDetails" resultType = "AccountVo">
    SELECT *
    FROM account
    WHERE userid = #{userid}
  </select>

  <update id="updateProfile">
    UPDATE account
    SET password = #{password},
        nickname = #{nickname}
    WHERE userid = #{userid}
  </update>

</mapper>